{
  "name": "q2-hmac-replay-protection",
  "version": "1.0.0",
  "description": "HMAC-SHA512 request authentication and anti-replay attack security framework",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "test": "playwright test"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
